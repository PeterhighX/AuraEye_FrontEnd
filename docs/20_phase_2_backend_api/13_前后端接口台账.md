# AuraEye 前后端接口台账

更新时间：2026-08-11  
维护范围：`MakeupChat` 前端与 AuraEye NestJS API 运行时契约。  
事实优先级：NestJS Controller/DTO/Service > Swift 实际调用 > OpenAPI > 历史执行文档。

## 维护规则

每次前端或后端完成接口相关工作并准备提交时，必须同步更新接口状态、字段、鉴权、状态码、对应代码位置和验证结果。未接入、废弃或仅规划的接口不得写成已可用。

状态含义：`已接入并匹配`、`后端可用但未接入`、`前端遗留`、`契约待同步`、`待开发`。

## 公共基线

- Base URL：`https://api-dev.peterhigh.xyz/v1`，Endpoint 常量不再包含 `/v1`。
- 成功 JSON：`{ "request_id": "...", "data": { ... } }`。
- 登录、刷新、退出、账号信息成功状态为 `200`；创建视觉任务为 `202`。
- 认证接口使用 JSON；视觉任务使用 `multipart/form-data`，文件字段为 `image`。
- 鉴权请求使用 `Authorization: Bearer <access_token>`；创建视觉任务还携带 `Idempotency-Key`。
- Bearer 请求首次收到 `401` 时，客户端只刷新一次并重放一次，不记录 token。

## 接口台账

| 路由和方法 | 前端调用与字段 | 后端响应 | 状态 |
| --- | --- | --- | --- |
| `GET /health/live` | 无生产调用 | `data.status, data.checked_at` | 后端可用但未接入 |
| `GET /health/ready` | 无生产调用 | 同上；未就绪时返回 Problem Details | 后端可用但未接入 |
| `POST /auth/login` | `request_id, username, password` | 200；token、user、features | 已接入并匹配 |
| `POST /auth/refresh` | `request_id, refresh_token` | 200；刷新后的会话 | 已接入并匹配 |
| `POST /auth/logout` | `request_id, refresh_token` | 200；`data.revoked` | 已接入并匹配 |
| `GET /auth/me` | Bearer Token | 200；user、features | 已接入并匹配 |
| `POST /demo/fixtures/resolve` | 无调用 | 已弃用兼容接口 | 后端可用但未接入 |
| `POST /vision/jobs` | multipart：`image, request_id, capability, options`；Bearer + Idempotency-Key | 202；VisionJob | 已接入并匹配 |
| `GET /vision/jobs/{job_id}` | Bearer Token | 200；VisionJob | 已接入并匹配 |
| `GET /vision/jobs/{job_id}/result-image` | Bearer Token | 200；`image/png` | 已接入并匹配 |

### `POST /vision/jobs` options

| capability | Swift 编码字段 | 后端严格校验 |
| --- | --- | --- |
| `face_analysis` | `{ "consent_version": "visual-analysis-test-v1" }` | `consent_version` 长度 1–128 |
| `item_recognition` | `{ "recognition_scope": { "allowed_categories": [...], "max_items": 3 } }` | 分类限 `eyeshadow_palette`、`eyeliner`、`makeup_brush`；`max_items` 为 1…3 |
| `makeup_render` | `{ "recipe_id": "..." }` | `recipe_id` 必填 |

## Release demo 联调约束

- Release 必须包含 `DemoFixtures` 文件夹、manifest 和四张 canonical JPEG；不得通过 `EXCLUDED_SOURCE_FILE_NAMES` 排除。
- `fixed_demo` 完全由 `/auth/me` 或登录响应的 `features.gallery_mode` 判断，不检查用户名。
- `fixed_demo` 的用户档案、快速开始、档案详情和化妆品识别入口不显示相机，只进入固定演示相册。
- 固定相册首次建档保存原始图片字节；重新分析从本地文件读取相同 `Data` 与 MIME，不经过 `UIImage` 重编码。
- 固定 demo 的旧档案若不是 canonical 人脸原图，不提交视觉任务，提示重新从演示相册选择。
- Release 构建阶段禁止 `AURAEYE_ENABLE_LOCAL_AUTH_STUB` 出现在 `SWIFT_ACTIVE_COMPILATION_CONDITIONS` 或 `OTHER_SWIFT_FLAGS`。
- 本轮未修改后端服务、账号权限、Feature 字段或 `/vision/jobs` 公共请求字段。

## 当前阻断项与遗留项

1. Release API Base URL 已配置为 `/v1` 基地址，Archive 后仍需复核最终 App Info.plist。
2. `/chat/messages`、`/makeup/generate`、`/makeup/recommendations` 尚无已确认 NestJS Controller，不得作为已上线接口调用。
3. Release 真机三条人脸流程必须在安装新的 Archive 后执行，记录脱敏 request_id，不记录 token、密码或图片内容。

## 本次变更记录

| 日期 | 范围 | 状态更新 | 验证 |
| --- | --- | --- | --- |
| 2026-08-11 | Release API 配置与接口核对 | Release base URL 已补齐；认证和统一视觉链路匹配 | Swift DTO 与 Xcode 配置静态核对 |
| 2026-08-11 | 会话恢复与试妆图片 | 首个 401 自动刷新并单次重放；试妆结果图使用 Bearer | XCTest 已覆盖 |
| 2026-08-11 | Release demo 资源与输入限制 | Release 不再排除 DemoFixtures；fixed_demo 五个业务入口隐藏相机 | Release Build 与 Archive 通过；Archive 内 manifest、四图及 SHA 已核验；待真机界面验收 |
| 2026-08-11 | 人脸原图复用 | 重新分析读取保存的原始 Data/MIME，并拒绝旧版非 canonical 文件 | 同字节请求测试通过；完整 XCTest 29 通过、0 失败、2 跳过 |
| 2026-08-11 | Release 认证保护 | 新增 Release 构建期 Stub 条件检查 | Release Build/Archive 通过；可执行文件无本地 Stub 标记；待真机线上登录验收 |
